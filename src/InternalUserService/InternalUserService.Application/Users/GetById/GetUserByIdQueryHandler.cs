using InternalUserService.Application.Abstractions.Authentication;
using InternalUserService.Application.Abstractions.Data;
using InternalUserService.Application.Abstractions.Messaging;
using InternalUserService.Domain;
using Microsoft.EntityFrameworkCore;
using SharedKernel;

namespace InternalUserService.Application.Users.GetById;

internal sealed class GetUserByIdQueryHandler(IInternalUserServiceDbContext context, IUserContext userContext)
    : IQueryHandler<GetUserByIdQuery, UserResponse>
{
    public async Task<Result<UserResponse>> Handle(GetUserByIdQuery query, CancellationToken cancellationToken)
    {
        if (query.UserId != userContext.UserId)
        {
            return Result.Failure<UserResponse>(UserErrors.Unauthorized());
        }

        UserResponse? user = await context.Users
            .Where(u => u.Id == query.UserId)
            .Select(u => new UserResponse
            {
                Id = u.Id,
                FirstName = u.FirstName,
                LastName = u.LastName,
                Email = u.Email
            })
            .SingleOrDefaultAsync(cancellationToken);

        if (user is null)
        {
            return Result.Failure<UserResponse>(UserErrors.NotFound(query.UserId));
        }

        return user;
    }
}
